It is required that the file PrintInfo.xml and the folder gait are put within the same directory where the executable files are made. 

For instance, put the PrintInfo.xml file and the gait folder in the directory build/bin within which exist executable files Aris_Control_demo and client. 
